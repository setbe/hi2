# c

Freestanding high-level portable layer (`namespace c::`).

`c` depends on `a + b`.

This layer must request explicit `b` capabilities (sockets/windowing/time/etc.)
instead of checking platform names directly.

